version: "v1.0-beta-task"
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1404

blocks:
  - name: "Example Block"
    task:
      jobs:
      - name: Test it out
        commands:
          - checkout
          - ls -lah
